<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">ルーム編集</h1>

  <%= render "form", room: @room %>

  <%= link_to "表示", @room, class: "lnk_btn" %>
  <%= link_to "戻る", rooms_path, class: "lnk_btn" %>
</div>
